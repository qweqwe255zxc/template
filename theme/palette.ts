/**
 * Дубль значений из theme/tokens.css — нужен там, где CSS-переменных
 * физически нет: favicon (SVG-роут), OG-картинка через next/og,
 * themeColor в <meta>.
 *
 * Важно: правишь палитру — меняй оба файла. Хексы больше нигде
 * в проекте быть не должно, в компонентах только токены и классы Tailwind.
 */
export const palette = {
  paper: "#f4f2ee",
  surface: "#ffffff",
  ink: "#1b1c19",
  inkMuted: "#5b5f5a",
  rule: "#e3e0d8",
  accent: "#16494f",
} as const;

export const paletteDark = {
  paper: "#141615",
  ink: "#f2f3ef",
  accent: "#79c2c6",
} as const;

/**
 * Шкала глубины — дубль --elevation-* из tokens.css (светлая тема,
 * тариф «Эконом»). Нужна там же, где и палитра: в рантайме без CSS
 * (og-картинка, письма из /api/contact), плюс как единственный список
 * значений, по которому видно всю систему теней не читая CSS.
 *
 * Значения намеренно почти невидимые: Эконом плоский. Настоящая
 * глубина живёт в elevationStandard ниже и включается пресетом.
 *
 * Геометрия у обеих шкал одна: короткая тень с малым разлётом, тариф
 * меняет только силу. Тень с разлётом в 20–60px читается как «объект
 * висит над страницей», и это главный визуальный штамп — см. коммент
 * к --elevation-* в tokens.css.
 *
 * В самих компонентах эти строки не используются: там только классы
 * .ui-card* и токены --card-* / --elevation-*.
 */
export const elevation = {
  none: "none",
  sm: "0 1px 1px 0 rgb(27 28 25 / 0.04)",
  md: "0 1px 2px -1px rgb(27 28 25 / 0.05)",
  lg: "0 2px 4px -2px rgb(27 28 25 / 0.07)",
  cardHover: "0 2px 6px -2px rgb(27 28 25 / 0.10)",
} as const;

/**
 * Дубль --elevation-* из блока [data-preset="standard"].
 * Та же контактная геометрия, что и в elevation выше, но заметнее:
 * «Стандарт» отличается от «Эконома» силой тени, поверхностью карточки,
 * радиусом и плашками — а не разлётом тени.
 */
export const elevationStandard = {
  none: "none",
  sm: "0 1px 1px 0 rgb(27 28 25 / 0.05)",
  md: "0 1px 1px 0 rgb(27 28 25 / 0.05), 0 2px 4px -1px rgb(27 28 25 / 0.07)",
  lg: "0 1px 1px 0 rgb(27 28 25 / 0.06), 0 4px 8px -2px rgb(27 28 25 / 0.09)",
  cardHover:
    "0 1px 1px 0 rgb(27 28 25 / 0.07), 0 6px 12px -3px rgb(27 28 25 / 0.11)",
} as const;

/**
 * Шкала радиусов — дубль --radius-* из tokens.css.
 * `card` / `control` / `media` / `pill` — не ступени шкалы, а РОЛИ:
 * какую ступень они означают, решает пресет тарифа. Здесь записаны
 * значения базы («Эконом»), в «Стандарте» это 12 / 8 / 8 / pill —
 * угол убывает вместе с размером объекта, а не задан одним числом
 * на всё сразу.
 */
export const radius = {
  sm: "0.125rem",
  md: "0.25rem",
  base: "0.5rem",
  lg: "0.75rem",
  xl: "1rem",
  "2xl": "1.5rem",
  full: "9999px",
  card: "0.25rem",
  control: "0.125rem",
  media: "0.125rem",
  pill: "0.125rem",
} as const;
