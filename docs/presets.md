# Тарифы оформления (пресеты)

Как из одного шаблона собрать плоский «Эконом» и объёмный «Стандарт».
Это главный документ по теме: если он расходится с кодом — верить коду
(`theme/tokens.css`, `app/globals.css`, `lib/preset.ts`) и поправить этот
файл.

> **На этом шаблоне собираются только два тарифа: «Эконом» и
> «Стандарт».** Весь документ ниже — про них, и агент, собирающий
> `site.config.ts`, выбирает `theme.preset` только между этими двумя
> значениями. «Бизнес» (индивидуальный сайт премиум-класса) шаблоном не
> покрывается вообще: такой проект пишется с нуля отдельным репозиторием,
> без единого компонента отсюда.

---

## 0. Короткая версия

```ts
// content/site.config.ts
theme: {
  darkModeToggle: false,
  defaultMode: "light",
  faviconGlyph: "•",
  preset: "standard",   // ← вот это
},
```

Одно поле. Дальше:

- **не трогать** ни один компонент,
- **не дописывать** `shadow-*`, `rounded-xl`, градиенты и `bg-white/80`
  в `className` секций,
- **не править** `theme/tokens.css` под тариф — блок
  `[data-preset="standard"]` там уже есть.

Что ещё нужно сделать руками — раздел 3 (короткий чек-лист) и раздел 4
(готовый пример конфига).

---

## 1. Почему «Стандарт» может выйти плоским

Не тени плохо подобраны — глубина держится на четырёх вещах одновременно,
и если хоть одна не подключена, тариф визуально не работает:

- `variant` карточки реально настраивается из конфига, а не хардкожен в JSX компонента;
- тени в токенах достаточно сильные, чтобы читаться на тёплой бумаге (не 5–6% альфы);
- у карточки своя поверхность, отдельная от фона секции (`--card-bg` ≠ фон секции, иначе белое на белом);
- сам тариф выражен в коде — `theme.preset` → атрибут `data-preset` на `<html>` → блок токенов в `theme/tokens.css` → дефолты `lib/preset.ts` — а не только в документации.

Всё это уже подключено в текущем коде; dev-предупреждение ловит плоскую
раскладку в дорогом тарифе — раздел 2 объясняет механику.

---

## 2. Как устроен пресет

Тариф меняет **две разные вещи**, и это важно не путать.

### 2.1. Пластику — чистым CSS, без участия компонентов

`theme.preset` → `data-preset` на `<html>` (`app/layout.tsx`) → блок
`[data-preset="standard"]` в `theme/tokens.css` переопределяет
«ручки глубины». Компоненты читают
только ручки и не содержат ни одного условия про тариф.

| Ручка | Эконом | Стандарт |
|---|---|---|
| `--radius-card` | 4px | 12px |
| `--radius-control` (кнопки, поля) | 2px | 8px |
| `--radius-media` (фото, карта) | 2px | 8px |
| `--radius-pill` (бейджи, прогресс) | 2px | таблетка |
| `--elevation-sm/md/lg/card-hover` | контактная тень, 4–10% альфы | та же геометрия, 5–11% и вторым слоем |
| `--card-bg` | фон своей секции | `--surface-card` (собственная поверхность) |
| `--card-shadow` / `--card-shadow-hover` | none / none | `md` / `lg` |
| `--card-lift` | 0 | 0 (подъём — только у кликабельной, `--card-hover-lift` −2px) |
| `--btn-shadow` / `--btn-lift` | none / 0 | none / 0 (реакция кнопки — смена заливки) |
| `--header-shadow` / `--header-blur` / `--header-bg` | none / none / непрозрачный | `sm` / none / непрозрачный |
| `--icon-tile-padding` | 0 (голая иконка) | 10px, фон — нейтральные 6% от текста секции |
| `--section-tint` / `--hero-tint` | none | none (подсветка снята, см. ниже) |
| `--control-*` (поля формы) | подчёркивание | коробка с заливкой и радиусом |
| `--accordion-gap/bg/shadow` | 0 / нет / нет | 12px / поверхность карточки / нет |
| `--media-shadow` | none | `sm` |
| `--popover-*` (тост, меню селекта) | 4px / без тени | 12px / `lg` |

Это работает **само**, независимо от того, что написано в `sections`.
Даже конфиг, скопированный один в один с экономовского, в тарифе
`standard` уже выглядит иначе: другие радиусы, собственная поверхность
и тень у карточек, плашки под иконками, поля формы коробками, аккордеон
карточками, тень хедера.

> **Чего «Стандарт» намеренно НЕ делает.** Тариф отличается глубиной, а
> не количеством эффектов. Из него убраны четыре вещи, которые делали
> любой сайт на шаблоне похожим на сгенерированный макет:
> - **длинная рассеянная тень** (разлёт 28–64px) — карточка теперь
>   касается страницы, а не висит над ней;
> - **подъём у некликабельных карточек и тень с подскоком у кнопок** —
>   подъём остался ответом на курсор у того, что реально кликается;
> - **акцентная радиальная подсветка секций и hero** — цветная дымка по
>   всей странице читается как цвет, а не как «глубина ниже порога»;
> - **матовое стекло у всех хедеров разом** — это свойство одного
>   варианта (`header.variant: "glass"`), а не тарифа.
>
> Токены (`--section-tint`, `--hero-tint`, `--btn-shadow`, `--card-lift`)
> никуда не делись и по-прежнему объявлены в `:root` со значением
> none/0 — вернуть любую из этих деталей для конкретного проекта
> по-прежнему можно одним правилом в `tokens.css`, не трогая компоненты.

### 2.2. Раскладку — через дефолтные `variant` секций

Глубину физически некуда положить в табличной раскладке: рамки там
рисует CSS-grid (`border-top` / `border-left` на ячейке), а не объект.
Тень можно дать только объекту — карточке. Поэтому в дорогом тарифе
почти всё, что может быть карточкой, должно ею стать.

`lib/preset.ts` — таблица «тариф → раскладка по умолчанию»,
`components/SectionRenderer.tsx` её применяет:

```ts
variant={section.variant ?? presetDefaults(preset).features}
```

| Секция | econom | standard |
|---|---|---|
| `hero` | `type-only` | `split` |
| `stats` | `band` | `grid` |
| `stats.containerVariant` | `flat` | `elevated` |
| `features` | `table` | **`cards`** |
| `steps` | `rail` | **`timeline-vertical`** |
| `gallery` | `table` | **`grid`** |
| `testimonials` | `quotes` | **`cards`** |
| `team` | `columns` | **`cards`** |
| `pricing` | `table` | **`cards`** |
| `faq` | `narrow` | `narrow` |
| `contact` | `split` | `split` |
| `contact.layout` | `plain` | **`cardContainer`** |

**Явно указанный в конфиге `variant` всегда главнее.** Это и есть
единственная оставшаяся ловушка: конфиг обычно копируют с готового
примера, где `variant` проставлен у каждой секции экономовским
значением — и дефолты тарифа не срабатывают вообще.

Поэтому в dev-режиме на плоскую раскладку в тарифе `standard` выводится
предупреждение с готовой заменой:

```
[preset] Секция "services" (features): variant="table" — плоская раскладка
в тарифе "standard". Глубину в ней показать негде. Поставьте
variant: "cards" или уберите variant совсем — тогда возьмётся вариант
тарифа. См. docs/presets.md.
```

Если такое предупреждение появилось — это баг конфига, а не шум.

---

## 3. Чек-лист сборки «Стандарта»

1. `theme.preset: "standard"` в `content/site.config.ts`.
2. **Убрать `variant` из секций** — пусть работает дефолт тарифа. Указывать
   `variant` только тогда, когда нужна раскладка, отличная от тарифной,
   и это осознанное решение.
3. Запустить `npm run dev`, открыть страницу, **прочитать консоль сервера**.
   Ни одного `[preset]`-предупреждения быть не должно.
4. Дать `hero` картинку (`image: "/images/hero.jpg"`) или виджет
   (`widget: { title, badge?, metrics }`). Без них `split` молча
   откатывается на `type-only`, и первый экран остаётся голой типографикой.
   Файл должен реально лежать в `public/images/` — внешние URL запрещены,
   см. `docs/section-system.md`, раздел 2.
5. Поставить `icon` каждому элементу `features` — в этом тарифе иконка
   получает акцентную плашку, и без неё карточка выглядит пустой.
6. Дать кейсам `status` и/или `tags` — плашки в тарифе становятся
   таблетками и работают как визуальный ритм.
7. Если есть `pricing` — отметить рекомендуемый тариф `featured: true`:
   он получит акцентную рамку и усиленную тень.
8. Подобрать акцент под бренд (`--palette-accent` в `theme/tokens.css` +
   `accent` в `theme/palette.ts` — оба файла, они дублируют друг друга).
   Петроль `#16494f` — это акцент демо-сборки, а не универсальный
   дефолт. Палитра и шрифты берутся из референсов клиента, и до тех пор
   пока референсов нет (2–4 ссылки/скриншота), трогать эти файлы
   запрещено — CLAUDE.md §5, шаг 0 и §1.1.
9. Если в секциях есть CTA `variant: "centered"`/`"boxed"` — поставить
   `theme.titleStyle: "centered"`. Это отдельная от пресета ось (дефолт
   `"standard"` действует независимо от `theme.preset`, тариф её не
   трогает): без неё заголовок этих двух CTA-вариантов мельче и перестаёт
   быть центрированным. Подробно — `docs/section-system.md`, раздел 1.

Чего **не** делать:

- не дописывать тени, радиусы, градиенты и цвета в `className` компонентов —
  любая такая правка ломает сразу оба тарифа (один получает лишнюю
  глубину, другой недостающую);
- не менять `--radius-sm` / `--radius-md`: на них свёрстана вся база,
  тариф двигает **роли** (`--radius-card`, `--radius-control`,
  `--radius-media`, `--radius-pill`), а не ступени шкалы;
- не трогать типографическую шкалу и ритм: тариф отличается глубиной и
  пластикой, а не другими кеглями и отступами;
- не включать `hoverEffect` у карточек, которые никуда не ведут — в
  наборе `cursor-pointer`, и он врёт пользователю. В «Стандарте»
  карточка и без него реагирует на курсор (`--card-lift`).

---

## 4. Пример: минимальный конфиг «Стандарта»

Показаны только те места, которые отличаются от экономовского конфига.
Обратите внимание: `variant` не указан почти нигде — это и есть смысл.

```ts
export const siteConfig: SiteConfig = {
  // ... brand / contacts / seo как обычно ...

  theme: {
    darkModeToggle: false,
    defaultMode: "light",
    faviconGlyph: "S",
    preset: "standard",
  },

  sections: [
    {
      id: "hero",
      type: "hero",
      surface: "paper",
      number: "01",
      rail: "Продукт · 2026",
      headline: ["Складываем данные", "в одну картину"],
      lead: "Аналитика, которую читает не аналитик.",
      actions: [
        { label: "Попробовать", href: "#contact", variant: "primary" },
        { label: "Как это работает", href: "#process", variant: "secondary" },
      ],
      // Вторая колонка hero: либо image, либо widget. Без одного из них
      // split откатится на type-only и первый экран будет пустым.
      widget: {
        badge: "Живые данные",
        title: "За последний квартал",
        metrics: [
          { label: "Источников подключено", value: "34" },
          { label: "Время до первого отчёта", value: "12 мин" },
          { label: "Покрытие данных", value: "92%", progress: 92 },
        ],
      },
    },

    {
      id: "stats",
      type: "stats",
      surface: "paper",
      number: "02",
      // variant и containerVariant не заданы → grid + elevated из тарифа
      items: [
        { value: "34", label: "интеграции из коробки" },
        { value: "12", suffix: " мин", label: "до первого отчёта" },
        { value: "99.9", suffix: "%", label: "аптайм за год" },
        { value: "480", label: "команд в продукте" },
      ],
    },

    {
      id: "features",
      type: "features",
      surface: "surface",
      columns: 3,
      number: "03",
      nav: "Возможности",
      eyebrow: "Что умеет",
      title: "Три вещи, ради которых это ставят",
      // variant не задан → cards из тарифа
      items: [
        {
          icon: "scale",           // иконка обязательна: в тарифе у неё плашка
          title: "Единый слой данных",
          text: "Подключаем источники и приводим их к одной модели.",
          points: ["34 готовых коннектора", "SQL поверх любого источника"],
        },
        // ...
      ],
    },

    {
      id: "process",
      type: "steps",
      surface: "paper",
      number: "04",
      // variant не задан → timeline-vertical (вертикальный таймлайн карточками)
      items: [
        { number: "01", title: "Подключение", text: "…", meta: "1 день" },
        // ...
      ],
    },

    {
      id: "pricing",
      type: "pricing",
      surface: "paper",
      number: "05",
      // variant не задан → cards
      items: [
        { name: "Старт", price: "0 ₽", unit: "/ мес", features: ["…"] },
        {
          name: "Команда",
          price: "9 900 ₽",
          unit: "/ мес",
          features: ["…"],
          featured: true,          // акцентная рамка + усиленная тень
          action: { label: "Начать", href: "#contact", variant: "primary" },
        },
        { name: "Бизнес", price: "по запросу", features: ["…"] },
      ],
    },

    {
      id: "contact",
      type: "contact",
      surface: "surface",
      number: "06",
      nav: "Контакты",
      title: "Оставьте заявку",
      // layout не задан → cardContainer (форма в поднятой карточке)
      fields: [
        { name: "name", label: "Имя", type: "text", required: true },
        { name: "email", label: "Почта", type: "email", required: true },
      ],
      submitLabel: "Отправить",
      consent: "Нажимая кнопку, вы соглашаетесь на обработку данных.",
      successTitle: "Заявка отправлена",
      successText: "Ответим в течение рабочего дня.",
      errorText: "Не удалось отправить. Напишите нам на почту.",
    },
  ],
};
```

---

## 5. Правило для новых компонентов и правок

Любая новая «дорогая» деталь — тень, подсветка, скругление, плашка,
подъём — заводится **только** как токен: нейтральное значение в `:root`
(это Эконом) и настоящее значение в блоке `[data-preset="standard"]`.
В компонент попадает имя роли, а не число.

Практически: в `components/` не должно быть ни `shadow-md`, ни
`rounded-xl`, ни `hover:-translate-y-1` — только классы слоя глубины
(`ui-card`, `ui-button`, `ui-control`, `ui-media`, `ui-media-raised`,
`ui-popover`, `ui-accordion`, `icon-tile`) и роли-радиусы
(`rounded-card`, `rounded-control`, `rounded-pill`). Эти классы живут в
`@layer components` в `app/globals.css` — то есть обычные утилиты
Tailwind (`border-fg`, `h-full`, `mt-*`) их по-прежнему перебивают.

Проверка, что правка не сломала Эконом: переключить `preset` на
`"econom"`, собрать страницу и сравнить разметку с предыдущей версией —
измениться должны только имена классов, но не структура и не
вычисленные значения (тень `none`, радиусы 2/4px, подъём 0).
